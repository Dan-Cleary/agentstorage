# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (only `.gitignore` and `LICENSE`). The `.gitignore` is a Node.js template, indicating the project will likely be a JavaScript/TypeScript application.

### Environment

- **Node.js**: Available via nvm (v22.x). No `.nvmrc` or `engines` field exists yet.
- **Package managers**: npm, pnpm, and yarn are all available.
- No `package.json`, source code, tests, or build configuration exist yet.

### When code is added

Once application code is added, update this section with:
- How to install dependencies (e.g., `npm install`)
- How to run the dev server
- How to run lint and tests
- Any non-obvious setup caveats
